# CoinGecko API Configuration
# Note: This app now uses localStorage for API key storage instead of environment variables
# API keys should be configured through the app's settings interface

# Instructions:
# 1. API keys are now managed through the app's settings
# 2. Get your API key from: https://www.coingecko.com/en/api/pricing
# 3. Configure the API key in the app's setup wizard or settings
